version: 2

models:
  - name: int_sales__order_items_enriched
    description: '{{ doc("int_sales__order_items_enriched") }}'
    columns:
      - name: order_id
        description: "Primary key, unique identifier for each order."
        tests:
          - unique
          - not_null